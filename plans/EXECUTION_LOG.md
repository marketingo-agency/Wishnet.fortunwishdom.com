# EXECUTION LOG — Omni three-plan program (Images → Videos → Audios)

> Single source of truth for execution progress. Structure per the mission prompt. Updated at every phase completion, after every deploy/migration, before risky ops, and before context compaction.

## Current position
- **Plan:** 2 (OMNI_VIDEOS_TRACK_PLAN.md)
- **Phase:** 11 — UI perfection pass (M)
- **Exact next action:** Per plan Phase 11: both omni themes ([[data-omni-theme=dark]_&]: discipline — grep any stray `dark:` in the new video surfaces), 375/768/1024/1440 behavior (flex-wrap on button clusters, grid columns), WCAG AA (amber/cyan text tones on light theme), focus-visible/cursor-pointer/reduced-motion (framer entrances already gated via useReducedMotion — verify all new wizards), loading/empty/error states on every new surface (players, VSDistribution rows, caption editor, pickers). Deferred-polish items from Phases 8-10: Animate + Repurpose extra sources (Files/omni-assets/Library), Animate captions affordance copy. Live ui-reviewer is auth-blocked → static self-review + fixes now, live pass in Phase 12/Part Two (the Plan-1 precedent). Gates: tsc/lint/tests. Then Phase 12 QA (security-auditor MANDATORY + images regression + adversarial verify) and Phase 13 Delivery (npm run build w/ dev server OFF, merge feat/omni-videos-track → main --no-ff, push → VPS, CLAUDE.md entry, MEMORY.md).
- **Exact next action:** Build the omni_videos surface stages 1-3: (5a) Stage 1 Scenario (import a completed video_scenario run OR quick-brief calling scenario-generate inline) + Stage 2 Storyboard & cast (keyframe review, tier/model pick — LongCat long-form draft vs LTX-fast short draft per Q5; per-scene + total cost via estimateSecondsCost/D-V3 math). (5b) Stage 3 Scenes: per-scene generation queue (sequential video-submit via callOmniVideo, batched video-poll every 3-4s — GEN-01 semantics: failed rows get Retry + never count as fulfilled), i2v from keyframes (start_asset_id; optional end = next scene's keyframe), per-scene re-roll/edit-prompt, approval gating; INTERIM TERMINAL: "scenes saved — audio & assembly land in Phase 6"; builtThrough(omni_videos)→3; activate the Scenario Studio "Send to Video Studio" handoff button (seeds an omni_videos run from the scenario, provenance in step_state). Update tests (builtThrough 3, handoff seeding). Gates: tsc/lint/tests; live 4-scene draft E2E deferred (auth) → Phase 12/Part Two.
- **Exact next action:** (1) DEPLOY `omni-video` via MCP — verify_jwt TRUE, entrypoint supabase/functions/omni-video/index.ts, import_map_path supabase/functions/import_map.json, files: omni-video/{index,video-specs,persist,scenario}.ts + omni/{fal-runner,context,llm}.ts + _shared/{cors,rate-limit,sanitize,token-budgets}.ts + import_map.json (ALL byte-verbatim from repo; byte-diff readback after). (2) DEPLOY `omni-finisher` — verify_jwt FALSE, files: omni-finisher/index.ts + omni-video/persist.ts + omni/fal-runner.ts + import_map.json; byte-diff readback. (3) ONLY AFTER finisher verified: cron migration (mirror 20260612120000): seed pulse_connections provider 'omni_video_finisher' secret + cron.schedule 'omni-video-finisher' */2 pg_net POST {action?, cron_secret}. (4) Tab-closed test HEADLESS: SQL-insert a video run (mode omni_videos, user 4867c456-…QA user) + omni_assets row (kind video, status generating, model_id bytedance/seedance-2.0/text-to-video, metadata.fal_request_id = 019f6ded-8459-7e81-bf82-52727f812fcc — the retention-proven completed probe) with updated_at older than 90s → wait a cron tick → verify row status done + storage object in omni-video bucket → then DELETE the test rows + object. (5) Log 2a/2b checkpoint. NOTE: video-poll thumbnails deferred to compose/extract-frame (deviation logged in the commit).

## Bootstrap record (2026-07-16, first session)
- `plans/EXECUTION_LOG.md` did not exist → FIRST session.
- Read in full: CLAUDE.md, MEMORY.md, all three plans incl. §6 Landmines.
- Base verified: `git log` head = `e2ba60b` on `main` (plans require base ≥ e2ba60b ✓). Working tree: only MEMORY.md modified + untracked plans/.
- **Deploy capability:** `npx supabase projects list` → NOT authenticated (LegacyPlatformAuthRequiredError). **→ ALL backend work via Supabase MCP** (`deploy_edge_function`, `apply_migration`, `execute_sql`) with the mandatory fidelity protocol: verbatim full file set per deploy → `get_edge_function` readback → byte-diff vs local → redeploy until identical → confirm verify_jwt (omni: true; content-library & omni-finisher: false). Every deploy + readback verdict logged here.
- Gate override active: per-phase gates = self-verified checkpoints; stop only on (a) unfixable acceptance failure, (b) hard external blocker, (c) budget cap.

## Checkpoint log
(one dated line per completed phase: acceptance verdict, deploy versions + readback verdicts, commit hashes)
- **2026-07-17 P2/Phase 10 — PASS (self-verified).** Commit 213289f. Repurpose & Enhance live, maximally reused: VRSource (own done video assets, newest 24, thumbs signed via chunked video-poll; **deviation logged: Files-upload + Content-Library sources deferred to polish** — consistent with Animate) → stage 2 = **VSDistribution reused wholesale** (16:9 master fans to TikTok/Reel/Short/FB/Pin with trim-to-cap + LTX reframe snapped 16:9/9:16/1:1 + honest notes + per-variant cost — the plan's 9:16+1:1 acceptance; 4:5 not in the conservative reframe set → snaps with note) → VREnhance (Topaz 2x/60fps ~$0.01/s lands AS the yt_longform hero variant; mmaudio SFX ~$0.001/s 30s-cap with 'use as working source' swap for silent drafts; first/middle/last thumbnails via sync extract-frame with open-to-save) → stage 4 = VSFinalizeVideo reused. **10b long→shorts clipping DEFERRED to its own mini-plan (the plan's own pre-approved stretch default).** ALL FIVE video modes now built: History resume + hub dispatch simplified to direct routing (the honest lands-later toast retired). builtThrough(video_repurpose)=4; tests repinned. Gate: tsc clean, lint 0/38, 120 tests. Live fan-out/upscale E2E (paid) deferred → Phase 12/Part Two.
- **2026-07-17 P2/Phase 9 — PASS (self-verified).** Commits f1b0bf5 (edge+twin) + 28e42ad (client). **DEPLOY: omni-video v4 (verify_jwt TRUE, byte-diff 14/14 IDENTICAL, boot proven via in-code 401).** Schemas FETCHED FIRST per §1.2 (fal get_model_schema): Kling AI Avatar v2 Pro = image_url + audio_url (+ optional prompt, no duration/aspect knobs — audio drives length); Seedance reference-to-video = prompt + image_urls ≤9 (@Image1..N) + the t2v enums; latentsync = video_url+audio_url. Edge v4: both models join VIDEO_MODEL_CONSTRAINTS (new refImagesKey/audioKey constraint fields, twinned client-side); video-submit resolves reference_asset_ids (OWN assets) + wishpedia_image_ids (public wishpedia-media via getPublicUrl — the omni variant-submit never-raw-URLs rule) into refUrls (first ref doubles as the avatar image) + audio_asset_id (signed OWN voiceover); UTILITY_ALLOWLIST += latentsync (default lipsync tier) + sync-lipsync/v3 (hero, $8/min shown honestly). Client: AnimateWizard 4 stages — ANSource (Wishpedia entry search → ≤9-image grid; **deviation logged: Files/Omni-asset/Content-Library sources deferred to Phase 11 polish, honest note in UI** — Wishpedia covers the Wishu acceptance) → ANDirection (Motion prompt w/ @Image guidance, or Talk script + ElevenLabs brand-voice picker) → ANGenerate (Motion = one hero video-submit on reference-to-video 9:16/1080p; Talk = the 3-hop chain on EXISTING actions: voiceover-render → poll → video-submit on Kling Avatar w/ audio_asset_id; GEN-01 retries; finisher covers closed tabs) → stage 4 reuses the Clips tail (network chips + footerless VSCaptions + VSFinalizeVideo). builtThrough(video_animate)=4; hub card live; URL-addressable; History routes in; tests repinned. Gate: tsc clean, lint 0/38, 120 tests. **Wishu fidelity sign-off (motion + talking clip, paid + eyes) → REQUIRES HUMAN at delivery (per plan).**
- **2026-07-17 P2/Phase 8 — PASS (self-verified).** Commit a3d921d. Clips mode live as a TRUE thin preset over Studio machinery (zero new edge actions — omni-video v3 already carries everything): ClipsWizard 4 screens — Idea (4 hook-first templates problem-hook/POV/listicle/before-after composing the 9:16 prompt; 6/10/15s length; 1-2 takes; engine cards Kling v3 Pro hero $0.14/s default / Seedance value calibrate-labeled / LTX-fast draft; up-front cost line incl. honest '≈/verify' for unit-priced Seedance) → Generate (useVideoScenes runner REUSED with new engine.aspect field for 9:16 — Studio keeps its 16:9 default; takes = scenes idx 1..N; GEN-01 retries; choose exactly ONE take → approved_asset_ids) → Captions & format (VSCaptions REUSED on the chosen clip via new nextLabel prop — SRT sidecar + transcribe; 9:16 network chips TikTok/Reel/Short/FB-Reel/Pin building video_variants pointing at the clip as-is, honest 'no reprocessing' note; continue gated on ≥1 network) → Finalize (VSFinalizeVideo REUSED verbatim — media_type video items/posts + FIN-01 honesty + Pulse link). builtThrough(video_clips)=4; hub card flipped live; ?mode=video_clips URL-addressable; History resumes route to the surface. **Deviation logged:** the plan's acceptance wording '30s TikTok clip' exceeds every native-audio engine enum (Kling/Seedance cap 15s, LTX 10s) — Clips bounds at 15s with an honest 'longer stories belong in Video Studio' note; multi-scene 30s+ content is exactly Studio's job. Gate: tsc clean, lint 0/38, 120 tests. Live paid clip E2E deferred → Phase 12/Part Two.
- **2026-07-17 P2/Phase 7 — PASS (self-verified).** Commits 4d59942 (edge) + bed9d14 (client) + this docs commit. **DEPLOYS: omni-video v3 (verify_jwt TRUE, byte-diff 14/14 IDENTICAL) + content-library v19 (verify_jwt FALSE preserved, full readback line-verified inline, boot proven via in-code 401).** Edge: video-finalize now resolves the OWN assembly asset's srt_path/thumb_path/duration_s server-side into item metadata (paths only per D-V8, never client-supplied — closes the Phase-6 finalize-extension gap as a logged deviation: shipped in Phase 7 BEFORE its gate, protocol satisfied); content-library media_type-aware (PostRow + 3 selects; video posts park honestly on dispatch with the D-V10 manual-Pulse message; queued-retry sweep is image-only so parked video rows don't churn every 5 min; bucket-aware signing files vs omni-video; library-asset-urls returns thumbs from the extract-frame sidecar; NEW mark-posted action records manual upload-post publishes). Client: VSCaptions (transcribe → editable segments via new videoCaptions lib segmentsFromTranscribe/toSrt (+6 unit tests) → SRT sidecar uploaded client-side under owner RLS to {uid}/omni-videos/{run}/{assembly}.srt, linked via patchAssetMetadata; optional, honest empty/failed states) + VSDistribution (OMNI_VIDEO_NETWORKS preset picker; per-preset plan chips trim-to-cap + LTX-2.3 reframe $0.10/s snapped to 16:9/9:16/1:1 w/ honest snap notes (vsReframe helper); sequential video-utility chains against polled rows; refs persisted at submit; GEN-01 Retry/Redo; 16:9-fits variants ship as the master, free) + VSFinalizeVideo (recap grouped by network, per-post captions, FIN-01 amber honesty note — every network 'saves to library, publishes via Pulse'; video-finalize call w/ assembly_asset_id; completed panel links to Pulse Library; idempotent re-entry honored). Pulse Library video-aware: media_type types, urls+thumbs, card thumbnail + Video badge, Sheet video players, LibraryPostRow 'Publish via Pulse' (upload-post profile pick → postType video → mark-posted) replacing Post-now for video posts (D-V10). builtThrough(omni_videos)=8 — the full Studio pipeline is live; jump tests repinned. Gate: tsc clean, lint 0/38, **120 tests**. Live E2E (paid transcribe/reframe + upload-post publish + eyes) deferred → Phase 12/Part Two.
- **2026-07-17 P2/Phase 6 — PASS (6a+6b self-verified).** Commits 5f9ba76 (edge) + f13dc12 (finisher) + 1f447e2 (video-finalize) + 0fc1b38 (client) + docstring normalize. **DEPLOYS: omni-video v2 (verify_jwt TRUE, byte-diff 14/14 IDENTICAL incl. the 2 new modules; boot PROVEN — legacy anon JWT passed the gateway and got OUR in-code 401) + omni-finisher v2 (verify_jwt FALSE, readback line-verified after normalizing one local docstring wrap; boot proven via in-code 401 on missing secret).** Edge shipped: `list-voices` / `voiceover-render` (whisper ttsLine + same-voice merge lifted verbatim, EdgeRuntime.waitUntil against a polled omni_assets row kind=audio metadata.kind:'voiceover', 80MB cap, ~150wpm duration_s) / `music-generate` (lyria2, kind=audio metadata.kind:'music') / `assemble-run` (merge-videos w/ EXPLICIT resolution+fps → music-bed metadata probe + PRE-TRIM (compose does not clip to keyframe duration — probe verdict) → compose for VO+music or merge-audio-video for one track → loudnorm best-effort → persist + extract-frame thumbnail, all one waitUntil job w/ honest assembly_notes) / `transcribe` (ElevenLabs Scribe on fal, wizper fallback — Phase 7 + Plan 3 consumer) / **`video-finalize` (the logged deviation: omni's finalize-run mirrored on omni-video w/ media_type 'video' on items+posts — zero omni redeploy)**. Fixed pre-identified gaps: falVideoResult now reads utility result shapes (bare video_url/audio_url strings + audio/media objects — compose/merge outputs would have failed the old parser); persist.ts audio mimes (mp3/wav) + RIFF/ID3/mp3-sync magic bytes; video-poll passes fal-id-less background rows through as 'generating' instead of failing them + returns thumb_url; **finisher v2 sweeps kind IN (video,audio)** so an abandoned paid music bed persists (voiceover/assembly rows excluded by the fal_request_id filter — their waitUntil worker owns them). Client: VSAudio (per-scene editable narration persisted into scenario, ElevenLabs voice picker, VO render + lyria2 music against usePolledAsset rows, both OPTIONAL, no-ducking guidance) + VSAssembly (the cut = approved drafts ∪ done heroes; per-scene Kling v3 Pro hero re-render $0.14/s i2v-when-keyframed w/ metadata.superseded_by tagged on the draft; assemble w/ 1080p/720p pick; re-assemble; Phase-7 interim terminal) + useVideoAudio (usePolledAsset, submit helpers persisting asset ids at creation, markSuperseded); VSScenes interim terminal → real Continue gate (needs ≥1 approved). builtThrough(omni_videos)=5; jump tests repinned. Gate: 114 tests, tsc clean, lint 0/38. Live VO/music/assembly E2E (ElevenLabs + fal spend + eyes) deferred → Phase 12/Part Two.
- **2026-07-17 P2/Phase 5 — PASS (5a+5b).** Commit b6ff443. Video Studio stages 1-3 live: VSScenario (import usable video_scenario runs — scenes-bearing only — or inline quick-brief), VSStoryboardCast (keyframe strip + 3 draft engines w/ Q5 auto-suggest: LTX-fast $0.06/s audio / LongCat $0.04/s long-form silent / Seedance-i2v calibrate "verify"; D-V3 cost line, no fabricated numbers), VSScenes (useVideoScenes runner: sequential video-submit + 3.5s batched video-poll, GEN-01 retry semantics, Stop, persisting surfaced, tab-close-safe note, per-scene approve, clip ids persisted at submit time), interim terminal panel ("audio & assembly land in Phase 6"). Scenario→Studio handoff ACTIVE (provenance scenario_source_run_id; scenario completes as an artifact). builtThrough(omni_videos)=3; jump tests pinned to the built range. Gate: 114 tests, tsc clean, lint 0/38. Live 4-scene draft E2E (paid) deferred → Phase 12/Part Two (transport already proven headless by the finisher test).
- **2026-07-17 P2/Phase 4 — PASS.** Commit e7564e2. Scenario Studio live end-to-end (client): ScenarioWizard (4 stages, video-ordinal persistence + high-water, clickable rail, load/error/no-scenario fallbacks), ScenarioBrief (topic/URL/paste + InspireMe + scene count → scenario-generate), ScenarioStructure (full scene editor + regenerate-all), ScenarioStoryboard (keyframes via EXISTING image pipeline: variant-submit flux/schnell 16:9 + batched variants-poll, per-scene re-roll, ~$0.002/frame note, resume-restore, asset ids persisted as soon as paid outputs exist), ScenarioExport (shot list + copy; Send-to-Video-Studio DISABLED w/ Phase-5 badge; Finish → status completed). builtThrough(video_scenario)=4; hub card live; URL-addressable; History routes scenario runs to their surface; jump tests extended (built vs unbuilt modes). Gate: 114 tests, tsc clean, lint 0/38. Live brief→storyboard E2E (LLM + image spend + eyes) deferred → Phase 12/Part Two; scenario-generate edge action already smoke-deployed in Phase 2. Deviation noted: InspireMe mines image-flavored ideas (video-flavored mining = Phase 11 polish candidate).
- **2026-07-17 P2/Phase 3 — PASS.** Commit 33eb7c5. **Videos track LIVE**: entry tile available (Videos before Audios), VideosHub six cards (five honest "lands in Phase N" inert stubs + live History), HistoryView `family` prop (images default — zero images-hub change; family filter in the dedup pass + mode-select options), historyRouting video branches (surface = own mode; steps/progress/reached clamped to builtThrough), OmniAgent videos routing (?track=videos&mode=history URL-addressable; resuming an unbuilt video workspace toasts honestly instead of mis-routing — landmine #10 first half). **Bucket-aware deletion (D-V8 second half): every run delete now clears BOTH files/omni-images AND omni-video/omni-videos folders with the same cross-run sparing check.** Gate: 114 tests (images-History matrix untouched + green), tsc clean, lint 0/38. Live delete-a-video-run storage check deferred (login) → Phase 12/Part Two; the deletion code path is the same list/spare/remove logic per bucket, self-verified.
- **2026-07-17 P2/Phase 2 — PASS (2a+2b).** Commits 8d5190a + 1defe69. **DEPLOYS: `omni-video` v1 (verify_jwt TRUE, byte-diff 12/12 IDENTICAL after ASCII-normalizing two local headers) + `omni-finisher` v1 (verify_jwt FALSE, 4/4 readback-verified) + cron migration 20260717060000 (applied AFTER deploy+verify — landmine #14; secret seeded, */2 sweep armed).** Actions live: video-submit (model allowlist + per-model translation + own-asset i2v signing + Heart digest for 'raw'), video-poll (CAS 'persisting' claim, ≤50MB in-request / waitUntil above / 200MB cap, completed-but-422 surfaced), video-utility (strict per-op key allowlist, media URLs confined to fal.media + project storage, compose tracks capped, sync metadata/extract-frame), scenario-generate (whisper skeleton + safeFetch SSRF port + context-engine grounding). Client: callOmniVideo. Smokes: gateway 401 / preflight 200 / finisher 401 on missing+bogus secret. **TAB-CLOSED TEST: PASS headless** — backdated 'generating' row pointing at the retention-proven fal request was claimed + persisted by the CRON SWEEP itself (row → done, finished_by:'finisher', 801,993-byte mp4 object verified in omni-video bucket) → D-V7 proven end-to-end. Test run deleted (cascade). Deviations logged: video actions in a separate omni-video function (payload ceiling); video-poll thumbnails deferred to compose/extract-frame. Gate: 114 tests, tsc clean, lint 0/38.
- **2026-07-17 P2/Phase 1 — PASS.** Commit 5a7f186. Migration 20260717050000 applied live + SQL-verified (10-mode CHECK, 6-status CHECK w/ 'persisting', omni-video bucket private w/ owner RLS + admin read, 2× media_type). Registry video layer additive (MODE_FAMILY, VIDEO_MODES ×5, VIDEO_SCHEMA_VERSION=1, builtThrough=0 everywhere, clampToBuilt/resolveVideoPosition/surfaceForRunMode, video branch in validateJumpTarget); OmniMode/OmniAssetStatus unions widened (+scenario types D-V2); RUN_MODE_META gained 5 video metas; OMNI_VIDEO_NETWORKS (D-V6); falVideoSpecs constraints + snap helpers; falPricing per-second/generation + calibrate + estimateSecondsCost (cost chips mark duration-priced assets unknown — never pixel-guessed); OMNI_TRACKS: Videos before Audios, still coming_soon. Gate: 114 tests (7 new video specs), tsc clean, lint 0/38, images regression green (all Plan-1 tests untouched + passing). P2/Phase 0 note: all probe verdicts recorded except retention (d) — closes at the ~04:10 wakeup before any Phase 2 work.
- **2026-07-17 P1/Phase 13 — DELIVERED.** Merge commit 1930f2c (--no-ff) pushed to main → VPS auto-deploy (GitHub Actions; a failed build leaves the old container running). Pre-merge `npm run build` GREEN locally (dev server off — landmine respected). CLAUDE.md delivery entry written (f69c6d8). MEMORY.md stays pointed at this log (program continues into Plan 2). Plan 1 final tally: 14 phases, 4 edge deploys byte-verified (omni v30/v31/v32 + content-library v18), 107 tests, 0 confirmed Critical/High at QA, $0 spent on paid generation (all live paid checks deferred to Part Two by design).
- **2026-07-17 P1/Phase 12 — PASS (QA gate met: 0 confirmed Critical/High).** Commits efdce3f (fixes) + this docs commit. Three parallel review agents over the FULL diff e2ba60b..HEAD: **security-auditor VERDICT PASS — 0 Critical / 0 High / 1 Medium / 2 Low** (Medium: service-role signing of client-bindable omni_assets.storage_path = cross-user read IF a path leaks — capped by unguessable UUID paths + owner-scoped storage RLS); **code-reviewer — 0 Critical, 2 Warnings + 1 Suggestion** (mode-blind migration clamp; cursor tie-drop after bulk archive; blob-URL leak); **ui static review (live ui-reviewer impossible — login blocked; honest substitute) — Score B, 2 Critical / 8 Warnings / 7 Suggestions**. ALL Criticals + the Medium + both Lows + both code Warnings + every high-value UI warning FIXED: stepRegistry handoff floor (prereq scan starts at the floor; v1+v2 branches; 3 new tests pin it — prod check: only 3 legacy runs, all omni_images, zero affected); composite (time,id) keyset cursor; unmount blob revoke; signStoragePath REQUIRES ownerId + namespace refusal; outpaint canvas cap 8192/48MP; fence-delimiter neutralization; tier-chip flex-wrap (C1); StageGenerate restore gate (C2 — double-billing race); source-picker error branches; brainstorm resume loading/error gate; aria-pressed conversions (radio/tab contracts removed); 32px touch targets; cursor-pointer on selects/checkboxes; theme-aware spinners + amber tile badge AA; catalog cursor reset on filter + honest "Next page" label; compare-modal late-candidate discard; History retry; InspireMe empty batch; thumb onError; badge aria-labels; reduced-motion guards on all 5 unguarded surfaces. **DEPLOY: omni v32 via MCP, verify_jwt=TRUE, readback byte-diff 16/16 IDENTICAL, preflight 200 from our handler.** Deferred (logged, non-blocking): useRunThumbs re-signs O(total) per new page (staleTime-cushioned; per-run caching later); v1 high-water rail can offer a harmless forward jump to an empty captions stage; StepFinalize caption-edit affordance (S3, design); prod `files` listing advisors (pre-existing SUP-04/SEC-009 backlog). Gate: tsc clean, lint 0/38, **107 tests**.
- **2026-07-17 P1/Phase 11 — PASS.** Commit 5214d46. Named backlog closed: SIB-10 (repurpose file-preview signing moved from render phase into a signingRef-guarded useEffect — render-time signing duplicated round-trips per re-render), SIB-09 (TStepFinalize links to /ai-agents/pulse?tab=library; stale copy gone), SIB-16 (transform handoff objective prefers transform_prompt over analysis.description), SIB-17 (Inspire-me error names Brain + Wishpedia as the sampled sources), SIB-15 (brainstorm persist failure → role=status amber "not saved yet" line; success clears it). Docstring drift: NONE found (WizardChrome/OmniImagesWizard already updated at the P7 flip). Contrast sweep: ALL `dark:` variants in src/components/omni/ converted to `[[data-omni-theme=dark]_&]:` (historyRouting badge metas, HistoryRunCard badges + step chips — `dark:` never tracked the page-local theme); Repurpose/Transform source-tab active pills (text-emerald-300/text-blue-300 raw) got light-mode -700 AA fallbacks. prefers-reduced-motion: global UI-019 CSS covers CSS animations; framer entrances match sibling-surface baseline. Gate: 104 tests, tsc clean, lint 0/38. Breakpoint screenshots + ui-reviewer live pass still auth-blocked → Phase 12 static + Part Two live.
- **2026-07-17 P1/Phase 10 — PASS.** Commit 4d3b488. Character Studio live: CharacterStudioPicker (entry search → canon grid, auto-attach 8 = FAL_EDIT_MODELS[0].maxRefs, hand-tune to 10, objective template, fuchsia accent, loading/empty/error states) → creates v2 omni_images run {origin:'character_studio', character_entry_id, reference_image_refs, schema_version:2} → Studio wizard Stage 1 via shared openStudioWithRun. Stage 2 default model = ALREADY-existing StageEngine behavior (curated[0]=nano-banana-pro/edit pre-selected when hasReferences, verified in code). Hub card flipped live; ?mode=character_studio URL-addressable; History origin badge; retake now preserves refs/origin/entry-id (real gap fixed — clones would have lost anchoring). Zero migration (grep: no schema change). Gate: 104 tests, tsc clean, lint 0/38. Wishu fidelity end-to-end (paid + eyes) → REQUIRES HUMAN at Delivery.
- **2026-07-17 P1/Phase 9 — PASS (9a+9b self-verified).** Commits 8ed35c6 (edge) + 9eca70d (client). **DEPLOY: content-library v18 via MCP, verify_jwt=FALSE confirmed.** Fidelity: v17's readback caught 5 comment dash-run transcription mismatches → normalized the file's decorative headers to ASCII `-----` (same convention as omni) → v18 readback matched local on EVERY line (headers, UUID_RE, new action block, glyph positions); unauthenticated probe returns OUR `{"error":"Unauthorized"}` = boots + dispatches. Shipped: edge `delete-items-by-run` (UUID-validated, admin-gated batch — 1 rate-limit slot/run, HIST-08); client History overhaul — cursor-paginated infinite scroll + sort select (HIST-09; title sort client-side, honest), 4-thumb strip + est-cost chip (new `estimateAssetsCost`: done+discarded billed, failed/no-model free, opaque/no-dims → hasUnknown) + meta line + reached-stage hint (HIST-10/14) + retake backlink badge via step_state.retake_of (HIST-15), batched createSignedUrls (HIST-07, chunked 100; asset query chunked 80 run-ids vs URL limits), select-all + bulk Archive (HIST-06), clear-all loops until empty w/ no-progress bail (HIST-05), delete checks items UNCONDITIONALLY + per-run warning toasts (HIST-04), RetakeRunDialog editable seed (title/objective/prompt/keep-models; edited prompt → provenance 'raw'; untouched prompt keeps provenance), search over objective/locked_prompt. Gate: 104 tests (+3 cost), tsc clean, lint 0/38. Acceptance: resume matrix green (unit); 200+ scroll + zero-stranded-items bulk-clear self-verified via code (browser/live items → Phase 12/Part Two).
- **2026-07-17 P1/Phase 8 — PASS.** Commits 6d0483f (edge + ASCII-fication) + client tier work. **DEPLOY: omni v31 via MCP, verify_jwt=TRUE confirmed. Readback byte-diff: ALL 16 FILES IDENTICAL (LF-normalized). Smoke: non-JWT bearer → gateway 401 (verify_jwt enforcing); OPTIONS preflight → 200 from OUR handler with allowlisted origin = bundle boots.** Shipped: edge `repurpose-submit` (tier-2 AI extend via fal-ai/flux-2-pro/outpaint — singular image_url + expand_{left,right,top,bottom}, NO prompt; expand math SERVER-side from STORED dims, srcW/H≥16 required, zero-expand <8px → 400 "use Smart crop"; asset metadata repurpose_tier:'extend'); client 3-tier engine (suggestRepurposeMode d<1.15 crop / ≤2.2 extend / >2.2 redesign REP-04; cropTrimFraction hint; ensureSourceDims measures+backfills; extend renders 'cover' plain downscale, redesign 'contain-blur' REP-06); StepRepurpose per-tile tier chips + suggested mode + pendingCost; RepurposeCompareModal tier radiogroup (candidate carries mode, approve adopts it); falPricing rows outpaint/bria/reframe; RepurposeModeWizard SIB-03 atomic v2 create + retry reuse + SIB-11 URL revocation. DEPLOY LESSON (permanent): the MCP deploy payload hit the output-token ceiling at ~147.6KB escaped → ASCII-fied decorative unicode in omni edge comments + collapsed 6+ dash runs (sanitize.ts untouched — functional dash regexes) → 143.6KB; partial-file-set deploys DO NOT work (bundler needs all 16 files; failed bundles never activate — v30 stayed live throughout). Gate: 101 tests, tsc clean, lint 0/38. Live paid extend verification (pixel fidelity, $) deferred → Phase 12/Part Two.
- **2026-07-17 P1/Phase 7 — PASS.** Commit 1061fdf. StageDistribution (networks+presets merged, DIM-01 multiplier + cost range, FIN-01 badges), StageCaptions (ORD-01 approval-gated, 1-per-image calls ×2 concurrency, CAP-01/02), StepFinalize FIN-02 inline edits + honesty. **THE FLIP DONE:** ACTIVE_SCHEMA_VERSION=2; persists write stage ordinals + stamp; migrate-on-read; WizardChrome 7-segment clickable labeled rail; transform handoff stamps v2 at stage 4 (stamp = boundary signal); repurposing born at stage 4 v2; retake clones born v2 (unlocked brainstorm clones unstamped); historyRouting/jump v2 branches; v1 behavior pinned. StepNetworks/StepDimensions/StepDescriptions/useOmniDescriptions deleted. Deviations logged: 3-tier adapt UI rides Phase 8; v2 transform runs jump only stages 4-7 (Retake covers redoing the transform). Gate: **101 tests** (incl. legacy matrix: step-8-no-presets→distribution, step-8-with-presets→captions, transform@7→distribution, repurposing@10→adapt, step-12→finalize, brainstorm routing), tsc clean, lint 0 errors/38 warnings (1 UNDER baseline). Live per-shape resume verification deferred (auth) → Phase 12/Part Two.
- **2026-07-17 P1/Phase 6 — PASS (6a+6b self-verified).** Commit c1038ae. StageBrief/StageEngine/StageGenerate live on v1 ordinals (1-2/3-4/5-6); old tail untouched; schema_version still 1; every run completable. Fixed: UX-01/04/05/06/07/08/09/10/11/14/15/17/18/20, GEN-01/02/03, SEL-01, ORD-02, GAP-7, UX-03 persist rollback. prompt_provenance persisted + sent (edge v30 injects digest for 'raw'). Old Step{Objective,LockPrompt,Specs,Recap} deleted (StepModels+StepGeneration kept for Transform; GEN-01 counting fixed there too). Deviation logged: 7-segment stage rail deferred to the Phase-7 flip (stage rail over unflipped v1 tail reads out of order — captions/dimensions inversion). Gate: tsc clean, lint 0/39 (SpecControls helpers split to specHelpers.ts to hold baseline), 91 tests green. Live cheap-model E2E deferred (auth blocker) → Phase 12/Part Two.
- **2026-07-17 P1/Phase 5 — PASS.** Commit c435b98. Context engine (omni/context.ts) built + all three prompt builders absorbed; brainstorm lock Heart-grounded (KB-GAP-4); SIB-06 provider fallback honors active_text_provider; variant-submit opt-in heartDigest via prompt_provenance (old-client byte-identical, 'promptor' skips, Heart failure blocks paid submit); generate-captions action + captions.ts + useGenerateCaptions hook (legacy captions path untouched); retrieveKnowledge now passes filter_agent_id='omni'; sampleKnowledge applies restricted_agents (data check: 2 restricted rows exist; RPC semantics mirrored). **DEPLOY: omni v30 via MCP, verify_jwt=TRUE confirmed. Readback byte-diff: ALL 16 FILES IDENTICAL (LF-normalized).** Smoke: no-auth→gateway 401; anon-JWT→our {"error":"Unauthorized"} = bundle boots + executes. Heart-cap note: live block ≈8.4k chars (1 critical + 33 medium) → 4k cap keeps critical + ~15 medium (plan-intended priority truncation; digest = the 1 critical rule). Functional live-verify (captions JSON, digest visible in fal prompt, brainstorm E2E) DEFERRED to Phase 12/Part Two (user-JWT blocker). Gate: 91 tests, tsc clean, lint 0/39. MCP-deploy lesson: full supabase/functions/... path layout + import_map.json + import_map_path REQUIRED (bare layout 400s); get_edge_function saves to tool-results file → scripted byte-diff works without context cost.
- **2026-07-17 P1/Phase 4 — PASS.** Commits 38bdb21 (structure) + f21d2b4 (rename, own commit for easy revert). Entry: 4 tiles unchanged + fal Engine bar below (showTestButton=false), F2 my-auto overflow fix, F5 inert coming-soon tiles. Hub: six cards 2×3 (Studio · Character Studio[disabled, P10] · Brainstorming · Transform & Upscale · Repurposing · History), F4 copy, icon dedup. Surprise fold: InspireMe (self-contained, queryClient-cached batch = SIB-05 fix) mounted in StepObjective; SurpriseMeView/SurpriseIdeaCard deleted; useStartRunFromIdea removed; ?mode=surprise_me→hub alias; legacy surprise runs open via History (RUN_MODE_META intact). F6 URL guard. F7 emerald contrast. Rename label-only: hub card/chrome/History badge/Pulse empty-state → "Studio"; osha-chat blurb edited (NOT deployed — rides next osha deploy). Q2+Q3 defaults ratified per gate-override. Gate: 91 tests, tsc clean, lint 0/39. Browser/theme/breakpoint verification deferred to Phase 11 (auth blocker). NOTE: plans/ + EXECUTION_LOG.md now committed on the branch.
- **2026-07-17 P1/Phase 3 — PASS.** Commit 6a2b8dd. stepRegistry.ts created (v1 identity layer + full v2 stage/migration layer, ACTIVE_SCHEMA_VERSION=1). All six consumers rewired (WizardChrome, TransformChrome, OmniImagesWizard, TransformWizard, historyRouting, useOmniHistory, HistoryRunCard, RepurposeModeWizard). HIST-03 fixed (brainstorm retake keeps messages+idea_locked); HIST-11 fixed (jump validated via validateJumpTarget + failures toast). 27 todo specs activated → 91 tests green; tsc clean; lint 0/39. Grep: only per-step JSX render branches remain (rebuilt in P6/P7); all sequence/boundary/floor/title knowledge in the registry. Zero visual change (Playwright smoke N/A — auth blocked; behavior pinned by the unit matrix instead). Note: v1 max_step_reached semantics — reaching old step 9 implies captions(6) as mapped high-water (walked step 8), test pinned.
- **2026-07-17 P1/Phase 2 — PASS.** Commit 32ba0cc. Vitest 4.1.10 (npm-verified current stable) + test/test:watch scripts. 50 passing tests: historyRouting resume matrix (18), falSpecs snapping (14), falPricing (11+), plus 27 it.todo D-REG registry specs (activate in Phase 3). tests/e2e/SMOKE.md documents the manual click-script honestly (headless auth blocked — plan 2.3's sanctioned fallback; automated port gated on QA creds). Gate: test green, tsc clean, lint 0/39. NOTE: CLAUDE.md's "Playwright 1.57" stack line is stale — not in package.json.
- **2026-07-16 P1/Phase 1 — PASS (code-only).** Commit 44e8ed1. New omni/llm.ts (isReasoningModel+openAiTuning); SIB-01 wired into brainstorm.ts (chat+lock), surprise.ts, analysis.ts (vision no-temp preserved, conclusion 0.4); fal-submit admin-gated + 8000-char cap (grep: zero client callers); finalize-run writes real run mode into item metadata. NO deploy (per Phase-0 verdict — batches into omni #2). tsc clean, lint 0/39. Acceptance reinterpreted per verdict: prod Transform is NOT broken today (gpt-4.1), fix is staged. Note: corrupt .next/dev/types/routes.d.ts from the background dev server broke tsc → killed server + rm -rf .next; keep dev server OFF unless needed.
- **2026-07-16 P1/Phase 0 — PASS.** Branch `feat/omni-studio-overhaul` created off e2ba60b. Baseline: tsc clean; lint 0 errors/39 warnings (matches documented baseline). SIB-01 verdict: **NOT live-reproducible** — prod `llm_settings.openai_text_model` = `gpt-4.1` (non-reasoning; accepts max_tokens/temperature); active_text_provider=openai; per plan rule Phase 1 deploys NOTHING, its changes batch into omni deploy #2 (Phase 5). Latent risk stands (switching the picker to gpt-5.x would break omni text paths until Phase 5's deploy). Q4 verdict: **zero** active heart_rules scoped promptor-but-not-omni → caption scope switch to omni is safe. `omni_settings` table has 0 rows (all defaults). Screenshot baseline: DEFERRED (see Open issues — QA login blocked). Dev server running on :8000 (background).

## Decisions & deviations
- CLI unauthenticated → MCP deploys with byte-diff readback protocol (mission STEP 0.3 authorizes this deviation from the plans' CLI preference).
- **P2/Phase 2 (2026-07-17): video edge actions live in a NEW `omni-video` function (verify_jwt=true, omni's exact in-function auth), NOT in omni.** Driver: hard MCP deploy-payload ceiling — omni's 16-file payload is at ~143.6KB escaped vs the ~147.6KB observed per-call output limit; adding video.ts/scenario.ts/fal-video-specs.ts (~20KB) would make omni undeployable via MCP. Same separation class the plan itself makes for omni-finisher (auth-driven there, payload-driven here). Consequences: client gets callOmniVideo (mirror of callOmni); omni itself needs NO redeploy this phase (zero risk to the live images track); every future video deploy stays small. persistFalMedia is not imported from _shared/fal.ts (would drag a ~14KB module into the bundle) — omni-video carries its own persistFalVideo mirroring the same host-validation/magic-byte/size-cap discipline (D-V8 intent preserved; letter deviation logged).

## Probe results
### Plan 2 Phase 0 (2026-07-17, via fal-ai MCP on Sam's key — paid calls logged in Budget)
- **Edge worker ceiling (docs, load-bearing for D-V7/D-V8):** wall clock 400s on paid plans (waitUntil bounded by the same 400s), memory 256MB, CPU 2s/request, request-idle timeout 150s (504 if no response). Confirms: VO renders fit waitUntil; video renders NEVER wait in-request; ≤50MB in-request persist threshold is safe against the 256MB isolate (arrayBuffer + upload copy ≈ 2-3× file size).
- **Seedance 2.0 t2v schema (re-verified live):** duration enum "4".."15"|"auto" (STRINGS), resolution 480p|720p|1080p|4k, aspect auto|21:9|16:9|4:3|1:1|3:4|9:16, generate_audio bool (no cost delta per schema note), bitrate_mode standard|high. Queue URLs confirmed at 2-segment app id (bytedance/seedance-2.0).
- **Probe A FINDING (load-bearing, new landmine):** benign prompt ("calm mountain lake at sunrise") with generate_audio=true → queue status COMPLETED but result fetch **422 content_policy_violation: "Output audio has sensitive content" (partner_validation_failed)**. Seedance's OUTPUT validation can reject arbitrary innocent clips at the RESULT stage → video-poll MUST treat completed-but-422 as a failed asset surfacing fal's detail (fal-runner's mapFalError already covers falResult 422s ✓); billing of rejected outputs unknown → REQUIRES HUMAN dashboard check. Audio-less retry submitted (019f6df3-c493…).
- **Probe B SUCCESS + persist math (D-V8):** 4s 480p 9:16 w/ audio = **801,993 bytes (~0.2MB/s at 480p)** → extrapolation: 15s 1080p hero ≈ 20-30MB (in-request OK), 60s 1080p final ≈ 90-120MB (finisher/waitUntil territory), 4k long-form breaches 200MB → cap + "split it" message validated. URL host `v3b.fal.media` (subdomain of fal.media ✓ host-validation compatible).
- **Probe A2 (audio-less retry) SUCCESS:** same benign prompt w/ generate_audio=false → 668,362 bytes 16:9. Confirms the probe-A rejection was specifically Seedance's AUDIO output validator. Mitigation for the build: audio-less drafts are immune; audio clips need the completed-but-422 failure path + user-facing retry.
- **Probe C (lyria2):** 6,291,544-byte WAV (~32.7s — Lyria2 beds are WAV not MP3; ~48kHz stereo). $0.10.
- **Probe (b) compose w/ mismatched inputs — VERDICTS (D-V4-shaping):** (1) it ACCEPTS mismatched aspects and normalizes onto a canvas from the FIRST video keyframe (output 864×496 ≈ clip 1's 480p 16:9; the 9:16 clip squeezed in) → scenes MUST be uniform-aspect before assembly (reframe first). (2) **keyframe `duration` does NOT clip media**: the 32.7s WAV under an 8000ms keyframe extended the container to 32.768s (video frames end ~8.1s) → ALWAYS pre-trim audio beds to the timeline length before compose. (3) Output container is **MOV** (`video/mov`, .mp4 filename), fps 23 → persist path needs a mov/quicktime mime mapping; finals should pass through a normalizing step (merge-videos w/ explicit resolution+fps, or loudnorm re-mux) for platform-safe mp4.
- **Probe (c) merge-audio-video — VERDICT:** schema = {video_url, audio_url, start_offset} ONLY. **No volume/ducking param** (D-V5 confirmed); the audio REPLACES the video's track.
- **Probe (e) persist stress — VERDICT (data + docs):** 480p ≈ 0.2MB/s (0.8MB/4s w/ audio); composed 8s draft = 1.2MB. Extrapolation: 15s 1080p hero ≈ 20-30MB → in-request persist OK; 60s+ 1080p finals ≈ 90-120MB → waitUntil/finisher only; 4k long-form breaches 200MB → cap + "split" message stands. Full persistFalMedia stress runs at Phase 2's deploy verification (needs the deployed action).
- **Probe (d) retention — VERDICT PASS (04:12):** probe B's queue result re-fetched 36 min after completion, byte-identical payload incl. the live fal.media URL → the D-V7 finisher (2-min sweep, 90s staleness) is comfortably viable; no eager-persist tightening needed. **PHASE 0 CLOSED — all five verdicts recorded.**
- DEVIATION (logged during the wait): Phase 1 foundations ran during the passive 30-min retention window; retention gates only Phase 2's finisher design.
- Unit-count calibration caveat: the stored fal key is API-scope (no billing read — proven 2026-06-14), and queue results don't carry billed units → if the result payload lacks billing info, real-$ calibration goes to REQUIRES HUMAN (fal dashboard usage page) and `calibrate:true` stays on Seedance pricing rows.

## REQUIRES HUMAN (accumulating)
1. **Confirm the QA user for Part Two** (one click): Dashboard → Authentication → Users → confirm `claude.qa.wishnet@gmail.com` (id 4867c456…) + grant admin (2026-05-21 audit pattern). Delete the user + test data after Part Two.
2. **Wishu / Character Studio fidelity sign-off** (paid generation + human eyes): pick Wishu in Character Studio → generate → confirm canon accuracy end-to-end.
3. **Live paid verifications** deferred from build phases: Heart digest visible in a 'raw'-provenance fal prompt; captions JSON quality per network; tier-2 AI extend pixel-fidelity on a real 1:1→16:9 job (flux-2-pro/outpaint ~$0.03/MP); tier-2 A/B vs bria/expand + ideogram/v3/reframe (both priced in falPricing, not wired).
4. **Token hygiene**: no new tokens were pasted this program (MCP-authenticated deploys); nothing to rotate so far.
5. **Orphan cleanup (one storage object):** the finisher tab-closed test left `omni-video/4867c456-57d7-4308-8f50-2f119518956c/omni-videos/9016ab88-0d43-42ee-bef1-70761f297492/9a28c6c1-bb5e-4d9c-a5a8-43eefd7cf60c.mp4` (~0.8MB; SQL deletes on storage.objects are trigger-blocked) — remove via Dashboard → Storage → omni-video when deleting the QA user.
6. **Seedance billing calibration:** check the fal dashboard usage page for the real $ of the 3 Phase-0 probe clips (4s 480p) and whether the content-policy-rejected clip billed — then replace the `calibrate: true` pricing rows.

## Budget spent
- Part One cap: $15. **Spent: ~$0.27 estimated** (Seedance units uncalibrated — API-scope key can't read billing; REQUIRES HUMAN dashboard check):
  - 2026-07-17 Plan2/P0 probe A: Seedance t2v 4s 480p (output REJECTED by content policy — billing unknown, est $0-0.05)
  - probe B: Seedance t2v 4s 480p 9:16 — est ~$0.05
  - probe A2: Seedance t2v 4s 480p 16:9 audio-less — est ~$0.05
  - probe C: lyria2 bed — $0.10
  - compose + metadata: ~$0.01

## Open issues
- **QA-admin provisioning blocked (2026-07-16, updated 07-17):** PROGRESS: GoTrue signup retry SUCCEEDED — QA user `claude.qa.wishnet@gmail.com` now EXISTS (id `4867c456-57d7-4308-8f50-2f119518956c`), status unconfirmed. Classifier still denies BOTH the `UPDATE auth.users SET email_confirmed_at` and even the password-grant login curl. REMAINING FOR SAM (one click): Supabase Dashboard → Authentication → Users → confirm `claude.qa.wishnet@gmail.com` (then grant admin per the 2026-05-21 audit pattern: profiles/user_roles) — unblocks the whole Part Two Playwright pass. The password was set this session (transcript); treat as temp — DELETE the QA user + its data after Part Two. Browser-gated acceptance items stay self-verified via tests + code inspection meanwhile.
