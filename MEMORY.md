# MEMORY - Working Memory

No active task. Ready for next assignment.

## Pending human actions
- Live visual pass: the sidebar AI Agents section should show ONLY Osha + Omni; confirm both agents' optimize wand + Settings work (auth-gated, headless login blocked).
- Rotate the fal key (standing item from earlier transcripts).

## Reference
Latest round 2026-07-23 (full record in CLAUDE.md audit history): six AI agents erased (Nexus/Promptor/Pixel/Pulse/Whisper/ATLAS) + the listing page; only Osha + Omni remain. optimize-draft ported into omni (v35) so both wands are self-contained; osha-chat v140 registry cleaned. migration 20260723120000 dropped 7 permission columns + 16 agent tables (applied post-push). 4 dead edge fns (promptor/pixel-chat/pulse-api/whisper-api) deleted; ai-chat kept (Settings test-connection infra). Supabase MCP was down -> CLI used for edge deploys/deletes.
