# MEMORY — Working Memory

## Current Task
Building the **Pulse** AI agent (Social Media Command Center) — a full multi-tab workspace at `/ai-agents/pulse` on upload-post.com + Meta Graph + Pixel/ElevenLabs/Canva. 10-phase plan, Sam approved "go for all". Full concept in [project_pulse_concept.md](../../Users/3issam/.claude/projects/c--My-Dev-Projects-Fortun-Wishnet/memory/project_pulse_concept.md).

## Plan Status
- [DONE] Phase 0 — Foundations: 4 tables (pulse_drafts/reply_queue/connections/settings) + RLS + indexes (migration 20260522120000); Pulse flipped to active agent; PulseAgent shell with 7 tabs (Overview·Create·Calendar·Posts·Engagement·Analytics·Settings); Settings tab reuses PulseSettings; types/pulse.ts domain types. tsc/lint clean.
- [DONE] Phase 1 — Connections & Settings: pulse-api v6 adds get/update/reset/test-connection-provider (Meta/ElevenLabs/Canva → pulse_connections, secrets server-side) + get/update-workspace-settings (pulse_settings). New hooks usePulseConnections + usePulseWorkspaceSettings + lib/pulseApi.ts. Components in components/pulse/settings/: PulseConnectionRow, PulseIntegrations, PulseReplyModel (separate provider+model+temp+persona), PulseAutomation (switchable manual/semi/auto + DM cap), PulseSettingsTab (composes these + existing PulseSettings). Wired into PulseAgent Settings tab. tsc/lint clean. ElevenLabs test is live; Meta/Canva validate-saved (OAuth deferred to their consuming phase).
- [DONE] Phase 2 — Posts Library + Drafts. Added the 4 pulse tables to integrations/supabase/types.ts (hand-written). usePulseDrafts (direct typed supabase client — pulse_drafts has authenticated-CRUD RLS, no edge needed): list w/ filters (status/platform/search) + create/update/delete. Components: pulse/pulseStatus.ts (status badges + PULSE_PLATFORMS + iso/local helpers), pulse/posts/PulseDraftEditor (Sheet: type/status/title/caption/platform chips/profile/schedule), pulse/posts/PulsePostsTab (toolbar filters + responsive card grid + edit/delete + AlertDialog). Wired into PulseAgent Posts tab. tsc/lint clean.
- [DONE] Phase 3 — Composer (Create tab). pulse-api v7 adds publish-post (multipart upload_text/upload_photos/upload — fields: user, platform[], title, description, photos[]/video as PUBLIC URL, scheduled_date, timezone; returns job_id). usePublishPost hook. PulseComposer (components/pulse/create/): profile datalist (from usePulseAccounts), platform chips, type, caption + "Improve with AI" (reuses useOptimizeDraft/Promptor), extended description, media URL list (photo/video), schedule toggle, Save draft / Publish now / Schedule (publish + records a pulse_drafts row). Wired into Create tab. tsc/lint clean. NOTE: media via public URL for now (Pixel direct-gen into composer = next enhancement); live publish needs Sam's upload-post profiles.
- [DONE] Phase 4 — Calendar/Planner: PulseCalendarTab (components/pulse/calendar/), @dnd-kit month grid, draggable post chips (locked when published/failed), droppable day cells + Unscheduled rail, drag to reschedule (sets scheduled_date + status→scheduled) or unschedule, month nav, status legend, click chip → PulseDraftEditor. Client-only (no deploy). tsc/lint clean.
- [DONE] Phase 5 — Bulk generation: PulseCreateTab toggle (Single/Bulk); PulseBulkGenerator (components/pulse/create/) — brief + count(3/5/8/10) + type + platforms → N caption variants via useOptimizeDraft (sequential, cancelable, progress), editable/removable variant list, Save all → pulse_drafts with shared campaign_id. tsc/lint clean.
- [DONE] Phase 6 — Engagement + AI replies. pulse-api v8 adds generate-reply (LLM via reply model OpenAI/Gemini + Heart rules + persona — FULLY FUNCTIONAL/testable with a queue row + OpenAI key), send-reply (Meta Graph comment/DM — needs page token), sync-engagement (Meta feed→pulse_reply_queue — needs page token; returns note if unconnected). Client: usePulseReplyQueue (list/generate/send/status/sync), PulseReplyCard (incoming + editable AI draft + Generate/Send/Skip + sentiment), PulseEngagementTab (Comments/Inbox sub-tabs + reply-mode switch + Sync). Wired into Engagement tab. tsc/lint clean. Meta send/sync go live once Sam connects a Meta page (OAuth — app approval pending).
- [DONE] Phase 7 — Analytics: PulseAnalyticsTab (components/pulse/analytics/) — profile select → per-platform metric grid (usePulseProfileAnalytics) + dependency-free SVG reach sparkline (recharts NOT installed — removed in audit). tsc/lint clean.
- [DONE] Phase 8 — Overview: PulseOverviewTab (components/pulse/overview/) — stat cards (scheduled/pending/published/connections, click to navigate), upcoming list, connection health, quick actions. Wired via onNavigate=selectTab.
- [ACTIVE] Phase 9 — QA: tsc clean + npm run lint 0 errors (36 known warns). Tightened pulse_drafts/reply_queue RLS → admin-only (migration 20260522140000). CLAUDE.md updated. Committing now. THEN run security-auditor agent (mandatory). NOT done: npm run build (dev live :8000), live publish/Meta verification (Sam creds).

## Key Decisions
- Engagement backbone = Meta Graph (FB+IG); upload-post = publish/schedule/analytics. NO Asana, Google Calendar, Freepik, subtitles.
- Reply model selected SEPARATELY from llm_settings; reply modes switchable anytime per profile.
- Every external integration = edge proxy (pulse-api pattern) + secret in pulse_connections (admin-only RLS) + client hook. MCP connections are dev-only, NOT prod.
- pulse-api currently at v5 (deployed via MCP, verify_jwt:false, bundles _shared/).

## Current State
Phase 0 shipped and live-clickable on dev :8000. Pulse workspace renders; Settings tab functional, other tabs are styled placeholders. Branch fix/audit-remediation (Pulse work is on top of it — NOT yet committed/pushed).

## Next Steps When Resuming
1. Build Phase 1: extend Connections (Meta/ElevenLabs/Canva editors + test), Reply-Model picker, Automation controls; edge actions in pulse-api or new fns.
2. Sam to supply (for live test, not to unblock build): Meta app id/secret, ElevenLabs key, Canva creds; start Meta app verification.
3. Continue per "go for all" — checkpoint at each phase boundary; QA-gate each.
